{
  "description": "Sum of even squares - comprehensive test case for all Julia strategies",
  "python_code": "sum(i*i for i in range(1, 10) if i%2==0)",
  "expected_result": 120,
  "test_cases": [
    {
      "name": "loops_sequential",
      "flags": {"mode": "loops", "parallel": false},
      "golden_file": "loops_seq.jl.golden",
      "description": "Sequential loops mode - baseline implementation"
    },
    {
      "name": "broadcast_sequential", 
      "flags": {"mode": "broadcast", "parallel": false},
      "golden_file": "broadcast_seq.jl.golden",
      "description": "Broadcast mode - vectorized implementation"
    },
    {
      "name": "loops_parallel",
      "flags": {"mode": "loops", "parallel": true},
      "golden_file": "loops_parallel.jl.golden", 
      "description": "Parallel loops mode - thread-local partials"
    },
    {
      "name": "dict_comp_sharded",
      "flags": {"mode": "loops", "parallel": true},
      "golden_file": "dict_comp_sharded.jl.golden",
      "description": "Dict comprehension with shard-merge pattern",
      "python_code": "{x: x*x for x in range(1, 10) if x%2==0}"
    }
  ],
  "validation": {
    "test_sizes": [0, 1, 5, 10, 100],
    "edge_cases": ["empty_range", "single_element", "all_odd", "all_even"]
  }
}
