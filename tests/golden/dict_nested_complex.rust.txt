// Rendered from IR (origin: python)
use std::collections::HashMap;
pub fn dict_nested_complex() -> HashMap<_, _> {
    let result = (1..4).flat_map(move |i| (1..4).filter(|&j| i != j).map(move |j| (i, j))).collect::<HashMap<_, _>>();
    result
}