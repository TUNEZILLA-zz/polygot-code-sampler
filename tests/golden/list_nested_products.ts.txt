// Rendered from IR (origin: python)
export function list_nested_products(): any[] {
  const result = (Array.from({length: (4 - 1)}, (_, i) => i + 1)).flatMap((i) => (Array.from({length: (4 - 1)}, (_, i) => i + 1)).map((j) => i * j));
  return result;
}