SELECT i * j
FROM (WITH RECURSIVE series(n) AS (SELECT 1 UNION ALL SELECT n+1 FROM series WHERE n < 3) SELECT n FROM series) AS i CROSS JOIN (WITH RECURSIVE series(n) AS (SELECT 1 UNION ALL SELECT n+1 FROM series WHERE n < 3) SELECT n FROM series) AS j WHERE i != j;