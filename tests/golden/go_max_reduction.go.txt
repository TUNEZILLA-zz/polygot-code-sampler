// Rendered from IR (origin: python)
func go_max_reduction() int {
    acc := -int(^uint(0)>>1)  // math.MinInt
    for i := 1; i < 5; i++ {
        for j := 1; j < 4; j++ {
            if i * j > acc { acc = i * j }
        }
        }
    return acc
}