// Rendered from IR (origin: python)
export function par_dict_squares(): Map<any, any> {
  const result = new Map((Array.from({length: (100 - 1)}, (_, i) => i + 1)).filter((i) => i % 2 == 1).map((i) => [ i, i * i ]));
  return result;
}