export function par_dict_squares(): Map<number, number> {
    return Array.from({length: 99}, (_, i) => 1 + i).filter(i => i % 2 == 1).reduce((map, i) => map.set(i, i * i), new Map<number, number>());
}
