# Generated by PCS: Julia backend
module PCS_Generated_e82c

    const PCS_Generated = PCS_Generated_e82c
    
    include("pcs_runtime.jl")
    using .PCS_Runtime
    
    function main()::Int
        # NOTE: parallelized with thread-local partials
        # NOTE: gensym reset at emit start for deterministic output
        # NOTE: actual thread count available via Threads.nthreads()
        # NOTE: loops selected to avoid allocations (filter present)
        parts1 = fill(0, nthreads())
        @threads for i in 1:9
            if i % 2 == 0
                parts1[threadid()] += i * i
            end
        end
        acc2 = 0
         for p in parts1
            acc2 += p
        end
        return acc2
    end

end # module
