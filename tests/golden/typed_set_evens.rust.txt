// Rendered from IR (origin: python)
use std::collections::HashSet;
pub fn typed_set_evens() -> HashSet<i64> {
    let result = (0..10).filter(|&x| x % 2 == 0).map(move |x| x).collect::<HashSet<_>>();
    result
}