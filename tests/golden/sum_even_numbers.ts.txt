// Rendered from IR (origin: python)
export function sum_even_numbers(): number|boolean {
  const result = (Array.from({length: (11 - 1)}, (_, i) => i + 1)).filter((x) => x % 2 == 0).map((x) => x).reduce((a,b)=>a+b,0);
  return result;
}