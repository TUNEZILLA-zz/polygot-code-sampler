// Rendered from IR (origin: python)
func go_parallel_max() int {
    acc := -int(^uint(0)>>1)  // math.MinInt
    for i := 1; i < 10; i++ {
        for j := 1; j < 10; j++ {
            if i * j > acc { acc = i * j }
        }
        }
    return acc
}