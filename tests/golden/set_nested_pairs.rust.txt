// Rendered from IR (origin: python)
use std::collections::HashSet;
pub fn set_nested_pairs() -> HashSet<_> {
    let result = (0..3).flat_map(move |i| (0..3).filter(|&j| i != j).map(move |j| (i, j))).collect::<HashSet<_>>();
    result
}