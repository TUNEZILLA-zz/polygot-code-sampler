// Rendered from IR (origin: python)
use std::collections::HashMap;
pub fn typed_dict_odds() -> HashMap<i64, i64> {
    let result = (1..6).filter(|&i| i % 2 == 1).map(move |i| (i, i * i)).collect::<HashMap<_, _>>();
    result
}